import { layoutType } from '@/consts'

const state = () => ({
  layoutType: layoutType.TOP_MENU
})

const getters = {
}

const mutations = {
}

const actions = {
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}
