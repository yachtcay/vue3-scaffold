import { layoutType } from '@/consts'

const state = () => ({
  layoutType: layoutType.SIDE_MENU
})

const getters = {
}

const mutations = {
}

const actions = {
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}
